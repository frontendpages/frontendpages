pre-commit:
  parallel: true
  commands:
    prettier:
      glob: "**/*.{css,ts,tsx,md,mdx}"
      run: |
        pnpm prettier --write {staged_files}
        git add {staged_files}

# Validate commit messages
commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
